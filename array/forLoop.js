const names = ['Alice', 'Bob', 'Charlie', 'David', 'Edward'];

for (let i = 0; i < names.length; i++) {
  console.log(names[i]);
}

console.log('-------- for of ---------');

const cars = ['Volvo', 'BMW', 'Lada', 'Audi', 'VW'];
for (let car of cars) {
  console.log(car);
}
